# Embedding + Milvus Lite 向量库依赖
# 安装：pip install -r requirements-embed.txt
#
# GPU（可选，加速 embedding）：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# 首次运行会自动下载 BAAI/bge-large-zh-v1.5

pymilvus>=2.4.4
milvus-lite>=2.4.4
sentence-transformers>=3.0.0
torch>=2.0.0
pandas>=2.0.0
tqdm>=4.66.0
