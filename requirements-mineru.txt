# 方案 B：MinerU PDF 解析依赖
# 安装：pip install -r requirements-mineru.txt
#
# GPU 使用（必须两步，缺一不可）：
# 1. 安装 CUDA 版 PyTorch（默认 pip 安装的往往是 CPU 版）：
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# 2. 在 src/parse_pdf_mineru.py 中设置 MINERU_DEVICE = "cuda"
#    设备通过环境变量 MINERU_DEVICE_MODE 传给 MinerU（新版 CLI 已无 -d 参数）
#
# 验证 GPU 是否生效：
#    python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#    应看到 True，且版本带 cu124 等后缀，而不是 2.x.x+cpu

mineru[core]>=2.0.0
pandas>=2.0.0
tqdm>=4.66.0
