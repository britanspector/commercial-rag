# commercial-rag 统一依赖（解析 + 分块 + 向量 + BM25 + Rerank）
#
# 建议先按本机环境安装 PyTorch（GPU/CPU 版本）：
# - GPU（示例，需按本机 CUDA/驱动选择）：pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# - CPU：pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# 然后安装本文件：
#   pip install -r requirements.txt

mineru[core]>=2.0.0
pandas>=2.0.0
tqdm>=4.66.0
transformers>=4.40.0
pymilvus>=2.4.4
milvus-lite>=2.4.4
sentence-transformers>=3.0.0
torch>=2.0.0
rank-bm25>=0.2.2
jieba>=0.42.1
FlagEmbedding>=1.2.10

# API 服务
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# 审计持久化（SQLite 默认；PostgreSQL 需另装 psycopg[binary]）
sqlalchemy>=2.0.0

# 生成质量评测 RAGAS（本地 Ollama + bge）
# LangChain 须同一 0.2.x 线：community 0.2.19 + openai 0.2.x + core 0.2.43
# 勿与 langchain-openai 1.x / langchain-community 0.4.x 混装（会 ImportError）
ragas>=0.2.10,<0.3
datasets>=2.14.0
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-text-splitters==0.2.4
langsmith>=0.1.112,<0.2
numpy>=1.26,<2
